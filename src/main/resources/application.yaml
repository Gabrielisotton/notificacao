spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username:
    password:
    protocol: smtp
    properties:
      mail:
        smtp:
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketfactory
            fallback: false
          auth: true
          starttls:
            enabel: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000
  thymeleaf:
    enabel: true
envio:
  email:
    remetente:
    nomeRemetente:

server:
  port:
